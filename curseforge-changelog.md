### 1.2.0 (2026-09-19)

- 新增 Minecraft 26.3（Wilderness Bound）支持，Fabric 与 NeoForge 双端同步发布。
- 修正模组列表元数据：主页 / 反馈按钮此前写在顶层而被加载器忽略，现已移入 `[[mods]]`，主页指向本模组 CurseForge 页、反馈指向仓库 Issues 与备用反馈站。
- 图标改用 26.2+ 的 `iconFile` 槽位（左侧列表 1:1 小图标），不再占用右侧详情横幅位；图标尺寸规范到 128×128。
- Fabric 端在 `fabric.mod.json` 补上此前缺失的 Fabric API 前置声明；未安装 Fabric API 时由加载器直接给出明确提示，而不是留到运行期报晦涩错误。
- 被拦截指令的聊天提示改为按玩家客户端语言解析并附同语言兜底文案，红色改由组件样式给出（旧式 `§` 色码在第三方 UI 下会静默失效）。
- 语言文件补齐 `en_gb`、`lzh`，共 15 种语言，键集合与 `en_us` 一致。
- 移除模组内多余的数据包描述文件，规避「数据包错误」导致无法进入世界。
